import React from 'react';
import { Alert, AlertDescription } from '@/components/ui/alert';

const SmartScheduling: React.FC<any> = () => {
  return (
    <Alert>
      <AlertDescription>
        O componente de agendamento inteligente está temporariamente desabilitado devido a atualizações no sistema.
      </AlertDescription>
    </Alert>
  );
};

export default SmartScheduling;